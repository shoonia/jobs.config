export const MAX_ITEMS = 20;

export const enum PERIOD {
  DAILY = 'Daily',
  WEEKLY = 'Weekly',
  MONTHLY = 'Monthly',
  CRON = 'Cron',
}

export const enum ROUTER {
  HOME = 'home',
  VALIDATOR = 'validator',
}
