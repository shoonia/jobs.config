import { h } from 'preact';

import s from './styles.css';

function Cron() {
  return (
    <fieldset className={s.fields}>
      <div className={s.location}>
        Cron
      </div>
    </fieldset>
  );
}

export default Cron;
